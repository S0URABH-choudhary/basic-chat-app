{
  "name": "frontend",
  "version": "1.0.0",
  "description": "front end for chat app",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "react-scripts --openssl-legacy-provider build"
  },
  "author": "",
  "license": "ISC"
}
